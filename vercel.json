{
  "buildCommand": "pnpm --filter @workspace/tictactoe run build && mkdir -p public && cp -r ./artifacts/tictactoe/dist/* public/ || true",
  "outputDirectory": "public",
  "installCommand": "pnpm install",
  "framework": null,
  "env": {
    "NODE_ENV": "production",
    "BASE_PATH": "/"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
