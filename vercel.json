{
  "buildCommand": "pnpm --filter @workspace/tictactoe run build; mkdir -p build_output; cp -r artifacts/tictactoe/build/* build_output/",
  "outputDirectory": "build_output",
  "installCommand": "pnpm install",
  "framework": null,
  "env": {
    "NODE_ENV": "production",
    "BASE_PATH": "/"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
